<div class="collection-link" destination="{{@collection}}" {{on "drop" this.handleDrop}} {{on "dragover" this.enableDrag}}>
    <button class="delete-item" id="delete-{{@collection}}" type="button" {{on "click" this.deleteThis}}>X</button>
    <div class="collection-drop-icon">
        <h1>📂</h1>
    </div>
    <h3><a href="collection/{{@collection}}" >{{titleCase @collection}}</a></h3>
</div>