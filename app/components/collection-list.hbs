<div id="your-collections">
    <CollectionForm @controller={{@controller}}/>
{{#each @collections as |collection|}}
    <CollectionBox @collection="{{collection}}" @controller={{@controller}}/>
{{/each}}
</div>